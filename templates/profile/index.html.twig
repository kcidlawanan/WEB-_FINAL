{% extends 'base.html.twig' %}

{% block body %}
<h2>My Profile</h2>

<p>Email: {{ user.email }}</p>
<p>Roles: {{ user.roles|join(', ') }}</p>

<a class="btn btn-warning" href="{{ path('app_change_password') }}">Change Password</a>

{% endblock %}
