{% extends 'base.html.twig' %}

{% block title %}🏠 {{ property.title }} - Lawanan Real Estate{% endblock %}

{% block body %}
<style>
    .property-details {
        max-width: 900px;
        margin: 60px auto;
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        overflow: hidden;
    }

    .property-image {
        width: 100%;
        height: 400px;
        object-fit: cover;
    }

    .property-info {
        padding: 30px;
    }

    .property-info h2 {
        font-weight: 700;
        color: #003366;
        margin-bottom: 15px;
    }

    .property-info .price {
        color: #198754;
        font-size: 1.4rem;
        font-weight: bold;
    }

    .property-info p {
        color: #555;
        margin-bottom: 10px;
        font-size: 1rem;
    }

    .category-tag {
        display: inline-block;
        background: #e7f1ff;
        color: #004085;
        padding: 6px 14px;
        border-radius: 20px;
        font-size: 0.95rem;
        font-weight: 500;
        margin-bottom: 15px;
    }

    .btn-back {
        display: inline-block;
        margin-top: 20px;
        border-radius: 30px;
        padding: 8px 20px;
    }

    .details-label {
        font-weight: 600;
        color: #003366;
    }

    @media (max-width: 768px) {
        .property-image {
            height: 250px;
        }
        .property-info {
            padding: 20px;
        }
    }
</style>

<div class="property-details">
    {% if property.image %}
        <img src="{{ asset('uploads/images/' ~ property.image) }}" alt="{{ property.title }}" class="property-image">
    {% else %}
        <img src="https://images.unsplash.com/photo-1502672260266-1c1ef2d93688?auto=format&fit=crop&w=800&q=80"
             alt="Default Property" class="property-image">
    {% endif %}

    <div class="property-info">
        <h2>{{ property.title }}</h2>

        {% if property.category %}
            <span class="category-tag">🏷 {{ property.category.name }}</span>
        {% else %}
            <span class="category-tag text-muted">🏷 Not specified</span>
        {% endif %}

        <p class="price">💰 ₱{{ property.price|number_format(2, '.', ',') }}</p>
        <p><span class="details-label">📍 Location:</span> {{ property.location }}</p>
        <p><span class="details-label">🛏 Bedrooms:</span> {{ property.bedrooms }}</p>
        <p><span class="details-label">📝 Description:</span></p>
        <p>{{ property.description }}</p>

        <a href="{{ path('app_property_index') }}" class="btn btn-outline-primary btn-back">⬅ Back to Listings</a>
    </div>
</div>
{% endblock %}
